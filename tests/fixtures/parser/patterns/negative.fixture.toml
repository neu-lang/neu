kind = "parser-expression-statement-pattern-fixture"
milestone = "M0013"
source = "docs/adr/ADR-0024-expression-statement-pattern-syntax.md"
category = "negative"

[[cases]]
name = "unsupported_match_or_when_pattern_context"
source_text = "when (value) { Result.Ok(x) -> x }"
expected_diagnostics = ["unsupported_pattern_form"]
recovery_boundary = "pattern recovery boundary"

[[cases]]
name = "malformed_pattern_arguments"
source_text = "Result.Ok(value,)"
expected_diagnostics = ["malformed_pattern"]
recovery_boundary = "right parenthesis"

[[cases]]
name = "unsupported_destructuring_pattern"
source_text = "{ left, right }"
expected_diagnostics = ["unsupported_pattern_form"]
recovery_boundary = "right brace"
