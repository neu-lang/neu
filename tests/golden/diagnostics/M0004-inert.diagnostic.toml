kind = "inert-diagnostic-shape"
milestone = "M0004"
compiler_behavior = "none"
severity = "error"
primary_span = "required"
secondary_spans = "optional"
notes = "optional"
safe_suggestions = "optional"
source = "docs/adr/ADR-0015-diagnostics-as-semantics.md"
purpose = "Proves diagnostic snapshot shape without asserting any compiler error."

