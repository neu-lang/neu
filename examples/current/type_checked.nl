package examples.current

public fun typeChecked() {
    val ready: Bool = true;
    val count: Int = 1;
    val label: String = "ok";
    val absent: Null = null;

    val source: Int = 1;
    val copy: Int = source;
    val grouped: Int = (source);

    var next: Int = count;
    next = source;
}
