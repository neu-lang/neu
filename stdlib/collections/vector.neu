package collections

// Initial nominal sequence surface. Operations are added with their accepted
// ownership, iterator, protocol, and allocation contracts.
class Vector<T> {
}

public test func vector_surface_is_available() {
    assert(true, "Vector is available from collections");
}
