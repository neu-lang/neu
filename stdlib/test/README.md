# `stdlib/test`

`Test` is the initial interface-backed annotation marker. It can be applied to
zero-argument, executable top-level functions with `@Test`; test discovery,
execution, reporting, and property extensions remain compiler/tooling
contracts outside this package.
