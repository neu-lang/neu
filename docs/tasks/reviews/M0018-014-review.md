# Review: M0018-014

## Review Metadata

- Task ID: `M0018-014`
- Milestone: `M0018`
- main-task review: `main-task review`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0018-014-parser-grouped-expression-metadata.md`
- Milestone file: `docs/milestones/M0018-type-checking-core.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0027-type-checking-core.md`
  - `docs/adr/ADR-0024-expression-statement-pattern-syntax.md`
- Diff or changed files:
  - `crates/compiler/src/parser.rs`
  - `crates/compiler/tests/parser.rs`
  - `docs/tasks/M0018-014-parser-grouped-expression-metadata.md`
- Test results:
  - `cargo test --workspace --all-targets`: pass, 148 tests
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `sh docs/tests/m0018-type-checking-core-accepted.sh`: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0018-014-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests unless main-task review approval is recorded.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passed as the final gate.

## Findings

No blocking findings.

## Spec Compliance

- Relevant spec sections:
  - `ADR-0024: Expression, Statement, And Pattern Syntax`
  - `ADR-0027: Type Checking Core`
- Relevant ADRs:
  - `ADR-0024`
  - `ADR-0027`
- Compliance decision: `compliant`
- Notes:
  - ADR-0027 includes grouped expressions in M0018. This task only exposes parser metadata needed by a later type-checking task.
  - The implementation does not type grouped expressions, add AST child storage, or change parsing semantics.

## Test Review

- Test-first evidence:
  - Pre-implementation `cargo test --workspace --all-targets` failed because `ParseOutput` had no `grouped_expressions` field.
- Ordinary test result:
  - Pass, `cargo test --workspace --all-targets`, 148 tests.
- Adversarial test result:
  - Pass, harness ran after ordinary tests and soundness report found no scope escape.
- CI result:
  - Pass: formatting, clippy, workspace tests, M0018 acceptance script, and M0002 workspace CI script.

## Scope Review

- In scope:
  - Added `ParsedGroupedExpression` metadata with outer grouped expression, inner expression, and source span.
  - Added parser tests for valid grouped expressions, nested source ordering, and malformed group exclusion.
- Out of scope:
  - None observed.

## Architecture And Maintainability Review

- Maintainability concerns:
  - None. The metadata follows existing parser side-table patterns.
- Simplicity concerns:
  - None. Source-order insertion is localized to grouped expression metadata and preserves AST construction behavior.
- Required specialty reviews:
  - `main-task adversarial check`

## Decision

Approve. The task provides parser input metadata for future grouped expression typing without introducing new type-checking behavior.

## Follow-Up

- Add a later M0018 task to type grouped expressions from this metadata and existing expression type tables.
