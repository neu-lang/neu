# Review: M0023-003

APPROVE

Inputs read: `docs/tasks/M0023-003-lifetime-escape-diagnostics.md`,
`docs/SPEC.md`, `docs/adr/ADR-0036-bootstrap-borrow-and-lifetime-analysis.md`,
`docs/milestones/M0023-borrow-and-lifetime-analysis.md`, and the changed
borrow files.

The implementation is scoped to ADR-0036 lifetime escape records and exact
region-equality diagnostics. Same-region uses are accepted; different-region
uses diagnose on the escape node with the borrow node as origin. It adds no
source syntax, reference types, nested region overlap, calls, returns,
captures, async, unsafe, or FFI behavior.

Validation passed: `cargo fmt --all --check`, `cargo clippy --workspace
--all-targets -- -D warnings`, `cargo test --workspace --all-targets`,
`sh docs/tests/m0023-lifetime-escape-diagnostics.sh`, adversarial check, and
`git diff --check`.

Residual risk: combining borrow conflict and lifetime diagnostics into a
single report remains a future M0023 task.
