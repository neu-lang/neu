# Review: M0018-017

## Review Metadata

- Task ID: `M0018-017`
- Milestone: `M0018`
- main-task review: `main-task review`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0018-017-assignment-statement-type-checking.md`
- Milestone file: `docs/milestones/M0018-type-checking-core.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0027-type-checking-core.md`
  - `docs/adr/ADR-0024-expression-statement-pattern-syntax.md`
- Diff or changed files:
  - `crates/newlang/src/type_check.rs`
  - `crates/newlang/tests/type_check.rs`
  - `docs/tasks/M0018-017-assignment-statement-type-checking.md`
- Test results:
  - `cargo test --workspace --all-targets`: pass, 158 tests
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `sh docs/tests/m0018-type-checking-core-accepted.sh`: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0018-017-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests unless main-task review approval is recorded.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passed as the final gate.

## Findings

No blocking findings.

## Spec Compliance

- Relevant spec sections:
  - `ADR-0027: Type Checking Core`
  - `ADR-0024: Expression, Statement, And Pattern Syntax`
- Relevant ADRs:
  - `ADR-0027`
  - `ADR-0024`
- Compliance decision: `compliant`
- Notes:
  - The helper checks only assignments whose target and value expression types are already known.
  - Compatibility follows ADR-0027 exact identity plus nullable exceptions.
  - Mismatches produce `type_mismatch` on the assigned value expression.

## Test Review

- Test-first evidence:
  - Pre-implementation `cargo test --workspace --all-targets` failed with unresolved import `newlang::type_check::type_assignment_statements`.
- Ordinary test result:
  - Pass, `cargo test --workspace --all-targets`, 158 tests.
- Adversarial test result:
  - Pass, harness ran after ordinary tests and soundness report found no scope escape.
- CI result:
  - Pass: formatting, clippy, workspace tests, M0018 acceptance script, and M0002 workspace CI script.

## Scope Review

- In scope:
  - Added `type_assignment_statements`.
  - Added compatibility checks for exact type identity, `Null` to nullable target, and non-null base value to nullable wrapper target.
  - Added mismatch diagnostics on value expressions and skip behavior for missing side-table entries.
- Out of scope:
  - None observed.

## Architecture And Maintainability Review

- Maintainability concerns:
  - None. The helper is pure and follows the existing side-table style.
- Simplicity concerns:
  - None. Compatibility is factored into a private helper with ADR-0027 rules only.
- Required specialty reviews:
  - `main-task adversarial check`

## Decision

Approve. The task implements accepted M0018 assignment checking without introducing inference, name resolution, mutability, ownership, borrow, or backend behavior.

## Follow-Up

- Integrate assignment statement checking into a later whole-function type-check orchestration task.
