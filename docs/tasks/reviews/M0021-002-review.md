# Review: M0021-002

- Decision: `approve`.
- Authority: `docs/SPEC.md` ADR-0033; ADR-0033; M0021 milestone.

The change is scoped to no-payload enum parsing, AST identity, and metadata.
Tests were written before the new API existed and demonstrate ordered variants,
empty enums, and rejected payload-shaped entries. No `when`, resolution,
duplicate semantic diagnostics, or exhaustiveness implementation was added.
Focused tests, validator, formatting, strict clippy, and all 220 workspace
tests pass.
