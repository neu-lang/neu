# Review: M0029-004

## Decision

Approve. The implementation lowers only the explicitly scoped checked integer
and direct-call forms, preserves source spans and operand order, and rejects
partial input. It does not rerun frontend checks or add MIR/backend behavior.
All required validation passed.
