# Review: M0022-004

APPROVE

Inputs read: `docs/tasks/M0022-004-use-after-move-diagnostics.md`,
`docs/SPEC.md`, `docs/adr/ADR-0035-bootstrap-ownership-and-move-analysis.md`,
`docs/milestones/M0022-ownership-and-move-analysis.md`, and the changed
ownership/example files.

The implementation is scoped to use-after-move diagnostics from existing
transfer records and resolved local uses. It records the invalid later use as
primary node and transfer source use as move origin. It does not add parser
orchestration, branch/path analysis, calls, returns, captures, borrowing, or
destructor behavior.

Validation passed: `cargo fmt --all --check`, `cargo clippy --workspace
--all-targets -- -D warnings`, `cargo test --workspace --all-targets`,
`sh docs/tests/m0022-use-after-move-diagnostics.sh`, adversarial check, and
`git diff --check`.

Residual risk: end-to-end pipeline orchestration remains a future M0022 task.
