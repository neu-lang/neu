# Review: M0035-010 Primitive HIR Operators

## Result

Pass.

## Findings

- HIR preserves logical, equality, and comparison operator identities.
- Unary `Not` is represented distinctly from bitwise complement.
- Parsed operand order is preserved.
- The M0028 executable-core boundary still defers `!` while retaining its
  integer-only unary contract.
- MIR deliberately rejects these new operators until their typed operation
  model is implemented; no semantic fallback was introduced.

## Required Gates

- Ordinary tests: pass.
- Adversarial checks: pass.
- Specification and milestone comparison: pass.
- CI: pass. Formatting, the prior and current validators, Clippy with warnings
  denied, all workspace targets (367 tests), and diff checks passed.
