# Review: M0018-018

## Review Metadata

- Task ID: `M0018-018`
- Milestone: `M0018`
- main-task review: `main-task review`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0018-018-accepted-expression-type-composition.md`
- Milestone file: `docs/milestones/M0018-type-checking-core.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0027-type-checking-core.md`
  - `docs/adr/ADR-0026-name-resolution-policy.md`
  - `docs/adr/ADR-0024-expression-statement-pattern-syntax.md`
- Diff or changed files:
  - `crates/newlang/src/type_check.rs`
  - `crates/newlang/tests/type_check.rs`
  - `docs/tasks/M0018-018-accepted-expression-type-composition.md`
- Test results:
  - `cargo test --workspace --all-targets`: pass, 161 tests
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `sh docs/tests/m0018-type-checking-core-accepted.sh`: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0018-018-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests unless main-task review approval is recorded.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passed as the final gate.

## Findings

No blocking findings.

## Spec Compliance

- Relevant spec sections:
  - `ADR-0027: Type Checking Core`
  - `ADR-0026: Name Resolution Policy`
  - `ADR-0024: Expression, Statement, And Pattern Syntax`
- Relevant ADRs:
  - `ADR-0027`
  - `ADR-0026`
  - `ADR-0024`
- Compliance decision: `compliant`
- Notes:
  - The helper composes only ADR-0027 included expression forms: literals, resolved names with supplied known types, and grouped expressions.
  - It does not run name resolution, infer types, produce assignment checks, add declaration signatures, or type unsupported expressions.

## Test Review

- Test-first evidence:
  - Pre-implementation `cargo test --workspace --all-targets` failed with unresolved import `newlang::type_check::type_m0018_accepted_expressions`.
- Ordinary test result:
  - Pass, `cargo test --workspace --all-targets`, 161 tests.
- Adversarial test result:
  - Pass, harness ran after ordinary tests and soundness report found no scope escape.
- CI result:
  - Pass: formatting, clippy, workspace tests, M0018 acceptance script, and M0002 workspace CI script.

## Scope Review

- In scope:
  - Added `type_m0018_accepted_expressions`.
  - Composed literal, resolved-name, and grouped-expression side-table typing.
  - Added fixed-point grouped expression propagation for nested groups whose inner type becomes known during composition.
- Out of scope:
  - None observed.

## Architecture And Maintainability Review

- Maintainability concerns:
  - None. The helper centralizes accepted expression table construction for later checks.
- Simplicity concerns:
  - None. The fixed-point loop is limited to grouped-expression propagation and terminates when no new group entries are added.
- Required specialty reviews:
  - `main-task adversarial check`

## Decision

Approve. The task composes accepted M0018 expression typing without broadening the language semantics.

## Follow-Up

- Use the composed expression table in later declaration initializer and assignment orchestration tasks.
