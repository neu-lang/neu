# Review: M0032-012

Decision: pass.

The change adds only the process boundary needed to run an explicit linked
output. It passes no CLI arguments, does not discover executables, and leaves
language exit mapping to the startup shim contract. Focused tests cover normal
exit and unavailable-output behavior.
