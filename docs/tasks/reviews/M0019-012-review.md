# Review: M0019-012

## Review Metadata

- Task ID: `M0019-012`
- Milestone: `M0019`
- main-task review: `main-task review`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0019-012-refined-expression-type-records.md`
- Milestone file: `docs/milestones/M0019-nullability-and-flow-typing.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0028-nullability-and-flow-typing.md`
- Diff or changed files:
  - `crates/compiler/src/type_check.rs`
  - `crates/compiler/tests/type_check.rs`
  - `docs/tests/m0019-refined-expression-type-records.sh`
  - `docs/tasks/M0019-012-refined-expression-type-records.md`
  - `docs/tasks/soundness/M0019-012-soundness.md`
- Test results:
  - `cargo test -p compiler --test type_check m0019_refined_expression_type_records`: pass, 4 tests
  - `cargo test -p compiler --test type_check`: pass, 56 tests
  - `sh docs/tests/m0019-refined-expression-type-records.sh`: pass
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `cargo test --workspace --all-targets`: pass, 195 tests
  - M0019 docs validators: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0019-012-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passes as the final gate.

## Findings

No blocking, high, medium, or low findings.

## Spec Compliance

- Relevant spec sections:
  - `SPEC.md` M0019 branch-scoped refinements and side-table output.
- Relevant ADRs:
  - `ADR-0028` branch boundaries, refined output, shadowing, nested scopes, and ambiguity diagnostics.
- Compliance decision: `compliant`
- Notes:
  - Exact `LocalBinding` equality controls whether a resolved use can inherit a refinement.
  - Name and block node kinds, source-file identity, and span containment enforce the active branch region.
  - Conditions, branch-external uses, and shadowing binding uses are excluded.
  - Nested branch-contained uses inherit the outer binding refinement.
  - Multiple applicable refinements produce `AmbiguousNullTestRegion` rather than an arbitrary type.
  - Original expression types and declaration signatures are not modified.

## Test Review

- Test-first evidence:
  - Focused tests initially failed because `record_m0019_refined_expression_types` did not exist, and the docs validator failed on the same missing API.
- Ordinary test result:
  - pass
- Adversarial test result:
  - pass
- CI result:
  - pass

## Scope Review

- In scope:
  - Added per-use refined expression side-table output.
  - Added exact binding, branch bounds, nested inheritance, shadowing, ambiguity, node-kind, and source-file tests.
  - Added a focused docs validator.
- Out of scope:
  - None observed. No nullable-use checking, assignment integration, invalidation, parser changes, spec changes, or example changes were made.

## Architecture And Maintainability Review

- Maintainability concerns:
  - none
- Simplicity concerns:
  - none; the pass composes existing refinement and resolution side tables without adding a control-flow graph or changing AST ownership.
- Required specialty reviews:
  - `main-task adversarial check`
  - `main-task specification check`
  - `main-task simplicity check`

## Decision

Approve.

## Follow-Up

- The next M0019 task should use these per-use views when checking nullable expressions in non-null-required contexts.
