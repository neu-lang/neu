# Review: M0018-020

## Review Metadata

- Task ID: `M0018-020`
- Milestone: `M0018`
- main-task review: `main-task review`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0018-020-type-rule-diagnostic-contract.md`
- Milestone file: `docs/milestones/M0018-type-checking-core.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0027-type-checking-core.md`
- Diff or changed files:
  - `crates/compiler/src/type_check.rs`
  - `crates/compiler/tests/type_check.rs`
  - `docs/tasks/M0018-020-type-rule-diagnostic-contract.md`
  - `docs/tasks/soundness/M0018-020-soundness.md`
- Test results:
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `cargo test --workspace --all-targets`: pass, 166 tests
  - `sh docs/tests/m0018-type-checking-core-accepted.sh`: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0018-020-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests unless main-task review approval is recorded.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passed as the final gate.

## Findings

No blocking findings.

## Spec Compliance

- Relevant spec sections:
  - `SPEC.md` ADR-0027 type checking diagnostics paragraph.
  - `ADR-0027: Type Checking Diagnostics`
  - `ADR-0027: Unsupported And Ambiguous Rules`
- Relevant ADRs:
  - `ADR-0027`
- Compliance decision: `compliant`
- Notes:
  - The change adds `unresolved_type_rule` and `unsupported_type_rule` diagnostic kinds.
  - Stable rule identifiers cover the ADR-0027 examples for missing annotation types, missing resolved name types, direct calls, structural function type application, members, binary expressions, and value-producing `if` expressions.
  - No new checker traversal or unsupported expression typing was added.

## Test Review

- Test-first evidence:
  - Pre-implementation `cargo test --workspace --all-targets` failed because the diagnostic kinds, constructors, and stable identifiers did not exist.
- Ordinary test result:
  - Pass, `cargo test --workspace --all-targets`, 166 tests.
- Adversarial test result:
  - Pass, harness ran after ordinary tests and soundness report found no scope escape.
- CI result:
  - Pass: formatting, clippy, workspace tests, M0018 acceptance script, and M0002 workspace CI script.

## Scope Review

- In scope:
  - Added `TypeRuleDiagnostic` stable rule identifiers.
  - Added unresolved and unsupported diagnostic kinds and constructors.
  - Preserved existing ambiguity and type mismatch behavior.
- Out of scope:
  - None observed.

## Architecture And Maintainability Review

- Maintainability concerns:
  - None. A unified stable rule identifier keeps diagnostics comparable while preserving existing ambiguity tests.
- Simplicity concerns:
  - None. The change is limited to the diagnostic data model.
- Required specialty reviews:
  - `main-task adversarial check`

## Decision

Approve. The task completes the ADR-0027 diagnostic representation needed by later M0018 fixtures without adding checker behavior.

## Follow-Up

- Later tasks may emit these diagnostics from a traversal pass, but must remain tied to accepted ADR-0027 unsupported and unresolved cases.
