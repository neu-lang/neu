# Review: M0018-015

## Review Metadata

- Task ID: `M0018-015`
- Milestone: `M0018`
- Reviewer: `Reviewer`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0018-015-grouped-expression-typing.md`
- Milestone file: `docs/milestones/M0018-type-checking-core.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0027-type-checking-core.md`
  - `docs/adr/ADR-0024-expression-statement-pattern-syntax.md`
- Diff or changed files:
  - `crates/newlang/src/type_check.rs`
  - `crates/newlang/tests/type_check.rs`
  - `docs/tasks/M0018-015-grouped-expression-typing.md`
- Test results:
  - `cargo test --workspace --all-targets`: pass, 151 tests
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `sh docs/tests/m0018-type-checking-core-accepted.sh`: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0018-015-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests unless reviewer approval is recorded.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passed as the final gate.

## Findings

No blocking findings.

## Spec Compliance

- Relevant spec sections:
  - `ADR-0027: Type Checking Core`
  - `ADR-0024: Expression, Statement, And Pattern Syntax`
- Relevant ADRs:
  - `ADR-0027`
  - `ADR-0024`
- Compliance decision: `compliant`
- Notes:
  - ADR-0027 states grouped expressions type to the inner expression type. The helper implements that rule only when the inner expression already has a type side-table entry.
  - The implementation preserves ADR-0027 side-table output and does not infer missing expression types or type unsupported expressions.

## Test Review

- Test-first evidence:
  - Pre-implementation `cargo test --workspace --all-targets` failed with unresolved import `newlang::type_check::type_grouped_expressions`.
- Ordinary test result:
  - Pass, `cargo test --workspace --all-targets`, 151 tests.
- Adversarial test result:
  - Pass, harness ran after ordinary tests and soundness report found no scope escape.
- CI result:
  - Pass: formatting, clippy, workspace tests, M0018 acceptance script, and M0002 workspace CI script.

## Scope Review

- In scope:
  - Added a pure `type_grouped_expressions` helper.
  - Added tests for type propagation, skipped unknown inner expressions, and already typed nested groups.
- Out of scope:
  - None observed.

## Architecture And Maintainability Review

- Maintainability concerns:
  - None. The helper follows the existing side-table helper pattern.
- Simplicity concerns:
  - None. The implementation performs only a direct side-table lookup and emit.
- Required specialty reviews:
  - `Adversarial Engineer`

## Decision

Approve. The task implements the accepted grouped expression typing rule without broadening M0018 behavior.

## Follow-Up

- Integrate grouped expression typing into later declaration initializer checks once the larger expression typing composition task is created.
