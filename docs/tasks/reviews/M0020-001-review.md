# Review: M0020-001

- Decision: `approve`.
- Authority: `docs/SPEC.md` ADR-0016 and ADR-0023; ADR-0016; M0020 milestone.

The change is limited to parser side tables for accepted generic parameter and
capability-bound syntax. Tests were written before the metadata API existed
and failed for that missing surface. The parser grammar and diagnostics remain
unchanged; no generic semantics or source-language feature was added. Focused
tests, task validator, formatting, strict clippy, and all 216 workspace tests
pass. Residual risk is intentionally deferred constraint meaning and solving.
