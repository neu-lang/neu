# Review: M0017-002

## Review Metadata

- Task ID: `M0017-002`
- Milestone: `M0017`
- Reviewer: `Reviewer`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0017-002-nullable-type-representation.md`
- Milestone file: `docs/milestones/M0017-type-representation.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0006-nullability-and-absence.md`
  - `docs/adr/ADR-0023-type-and-generic-syntax.md`
- Changed files:
  - `crates/newlang/src/types.rs`
  - `crates/newlang/tests/types.rs`
  - `docs/milestones/M0017-type-representation.md`
  - `docs/tests/m0017-nullable-type-representation.sh`
- Test results:
  - `cargo test -p newlang --test types`: pass
  - `sh docs/tests/m0017-nullable-type-representation.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0017-002-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passed as the final gate.

## Findings

No blocking findings.

## Spec Compliance

- Relevant spec sections:
  - ADR-0006: Nullable types are semantically explicit optional values and non-nullable types never implicitly contain null.
  - ADR-0023: Nullable syntax is postfix `?` over a primary type.
- Relevant ADRs:
  - ADR-0006
  - ADR-0023
- Compliance decision: `compliant`
- Notes:
  - The implementation represents nullable types as a distinct type record wrapping an existing `TypeId`. It does not implement checking, flow typing, null literal typing, or generic nullability constraints.

## Test Review

- Test-first evidence:
  - `cargo test -p newlang --test types` failed before implementation because `NullableType`, `TypeKind::Nullable`, and `TypeRecord::nullable` were absent.
- Ordinary test result:
  - pass
- Adversarial test result:
  - pass
- CI result:
  - pass

## Scope Review

- In scope:
  - `NullableType` wrapper over `TypeId`.
  - `TypeKind::Nullable`.
  - `TypeRecord::nullable`.
  - Tests proving wrapper identity and distinction from base types.
- Out of scope:
  - No flow typing, smart casts, null checking, parser changes, FFI null mapping, inference, constraints, ownership, layout, ABI, HIR, MIR, or backend code.

## Architecture And Maintainability Review

- Maintainability concerns:
  - none
- Simplicity concerns:
  - none
- Required specialty reviews:
  - `Adversarial Engineer`

## Decision

Approve. The representation is minimal and matches the M0017 nullable acceptance criterion.

## Follow-Up

- Add a separate M0017 task for unsupported type-form blocking.
- Add a separate ambiguity or acceptance task for primitive scalar categories.
