# Review: M0019-013

## Review Metadata

- Task ID: `M0019-013`
- Milestone: `M0019`
- main-task review: `main-task review`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0019-013-refinement-aware-assignment-checking.md`
- Milestone file: `docs/milestones/M0019-nullability-and-flow-typing.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0027-type-checking-core.md`
  - `docs/adr/ADR-0028-nullability-and-flow-typing.md`
- Diff or changed files:
  - `crates/compiler/src/type_check.rs`
  - `crates/compiler/tests/type_check.rs`
  - `docs/tests/m0019-refinement-aware-assignment-checking.sh`
  - `docs/tasks/M0019-013-refinement-aware-assignment-checking.md`
  - `docs/tasks/soundness/M0019-013-soundness.md`
- Test results:
  - `cargo test -p compiler --test type_check m0019_refinement_aware_assignment`: pass, 7 tests
  - `cargo test -p compiler --test type_check`: pass, 64 tests
  - `sh docs/tests/m0019-refinement-aware-assignment-checking.sh`: pass
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `cargo test --workspace --all-targets`: pass, 203 tests
  - M0019 docs validators: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0019-013-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passes as the final gate.

## Findings

Resolved medium finding:

- The first shared-loop implementation changed M0018 `T? -> T` from `TypeMismatch` to the M0019 nullable-use diagnostic. The fix added explicit private mode separation and a regression test proving `type_assignment_statements` retains M0018 behavior.
- Independent adversarial review found that a fabricated type-consistent refined view could authorize an assignment outside its guarded branch. The fix now validates unique flow provenance, exact resolved binding identity, and AST branch containment, with producer-composition and forged-region regression tests.

## Spec Compliance

- Relevant spec sections:
  - `SPEC.md` ADR-0027 assignment compatibility and M0019 nullable-use diagnostics.
- Relevant ADRs:
  - `ADR-0027`
  - `ADR-0028`
- Compliance decision: `compliant`
- Notes:
  - Valid unique per-use `T` refinements allow an originally `T?` assigned value to satisfy a `T` target.
  - Unrefined `T? -> T` reports `InvalidNullableUse` with `NullableAssignmentWithoutRefinement` at the value node.
  - `Null -> T`, unrelated `U? -> T`, exact matches, and nullable-target exceptions retain ADR-0027 behavior.
  - Inconsistent or duplicate refined side-table entries are not trusted.
  - Refinement-region linkage, exact binding resolution, and AST containment are revalidated before assignment consumes a view.
  - Assignment output records only the effective type and does not mutate original expression types.

## Test Review

- Test-first evidence:
  - Focused tests initially failed because `type_m0019_assignment_statements` did not exist, and the docs validator failed on the same missing API.
- Ordinary test result:
  - pass
- Adversarial test result:
  - pass
- CI result:
  - pass after both review fixes

## Scope Review

- In scope:
  - Added a refinement-aware M0019 assignment entry point through a shared internal loop.
  - Added diagnostic-boundary, compatibility-preservation, inconsistent-view, duplicate-view, producer-composition, and forged-region tests.
  - Added a focused docs validator.
- Out of scope:
  - None observed. M0018 public behavior, local initializers, grouped refinement propagation, invalidation, orchestration, parser, spec, and ADR files were unchanged.

## Architecture And Maintainability Review

- Maintainability concerns:
  - none
- Simplicity concerns:
  - none; one shared assignment loop prevents compatibility drift while public M0018 and M0019 entry points remain explicit.
- Required specialty reviews:
  - `main-task adversarial check`
  - `main-task specification check`
  - `main-task simplicity check`
  - `main-task diagnostics check`

## Decision

Approve.

## Follow-Up

- Apply the same nullable-to-base diagnostic and valid per-use refinement logic to local declaration initializers.
