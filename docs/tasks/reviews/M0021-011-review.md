# Review: M0021-011

**APPROVE**

The change implements only ADR-0033/0034 subject validation via resolved
bindings. It reports `invalid_match_subject` on invalid subject nodes and
defers arm and coverage semantics. Test-first evidence, validator, formatter,
strict Clippy, workspace tests, and adversarial check pass.
