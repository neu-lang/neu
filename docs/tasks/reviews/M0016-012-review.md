# Review: M0016-012

## Review Metadata

- Task ID: `M0016-012`
- Milestone: `M0016`
- main-task review: `main-task review`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0016-012-local-binding-storage.md`
- Milestone file: `docs/milestones/M0016-name-resolution-pass.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0026-name-resolution-policy.md`
  - `docs/adr/ADR-0015-diagnostics-as-semantics.md`
- Diff or changed files:
  - `crates/compiler/src/name_resolution.rs`
  - `crates/compiler/tests/name_resolution.rs`
  - `docs/tests/m0016-name-resolution-data-model.sh`
  - `docs/tasks/M0016-012-local-binding-storage.md`
- Test results:
  - Test-first failure: `cargo test -p compiler --test name_resolution` failed before implementation because local binding APIs were missing.
  - Ordinary tests: `cargo test -p compiler --test name_resolution && docs/tests/m0016-name-resolution-data-model.sh && docs/tests/m0016-name-resolution-policy-accepted.sh` passed.
- Adversarial report:
  - `docs/tasks/soundness/M0016-012-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests unless main-task review approval is recorded.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passed as the final gate.

## Findings

No blocker, high, medium, or low severity findings.

## Spec Compliance

- Relevant spec sections:
  - Name resolution source of truth is ADR-0026 for this slice.
- Relevant ADRs:
  - `ADR-0026-name-resolution-policy`
  - `ADR-0015-diagnostics-as-semantics`
- Compliance decision: `compliant`
- Notes:
  - The implementation adds local binding storage only. It does not invent lookup, parser extraction, imports, member lookup, overload resolution, or type-directed resolution.

## Test Review

- Test-first evidence:
  - Focused test failed before implementation with unresolved imports for `LocalBinding`, `LocalBindingIndex`, `LocalBindingInsert`, `LocalBindingKey`, `LocalBindingKind`, and `LocalScopeId`.
- Ordinary test result:
  - pass
- Adversarial test result:
  - pass
- CI result:
  - pass

## Scope Review

- In scope:
  - Local scope identifier.
  - Local binding key, kind, record, insert result, and index.
  - Duplicate same-scope local binding preservation.
  - Same name in distinct scope storage.
- Out of scope:
  - No parser local binding metadata.
  - No lexical scope construction.
  - No local lookup.
  - No new diagnostics for local duplicate emission.

## Architecture And Maintainability Review

- Maintainability concerns:
  - none
- Simplicity concerns:
  - none
- Required specialty reviews:
  - `main-task adversarial check`

## Decision

Approve.

## Follow-Up

- Add parser metadata for local `val` and `var` bindings in a later M0016 task.
