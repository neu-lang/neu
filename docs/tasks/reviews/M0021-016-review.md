# Review: M0021-016

APPROVE

Inputs read: `docs/tasks/M0021-016-exhaustive-match-coverage.md`,
`docs/milestones/M0021-algebraic-data-and-exhaustiveness.md`, ADR-0033
authority excerpt, and the changed compiler/test/example files.

The change is scoped to finite no-payload enum match coverage. It diagnoses
missing coverage on the subject, accepts complete and wildcard cases, and
suppresses cascades when prior resolution or duplicate-arm diagnostics already
make the match invalid. Test-first failure is recorded in the task log.

Validation passed: `cargo fmt --all --check`, `cargo clippy --workspace
--all-targets -- -D warnings`, `cargo test --workspace --all-targets`,
`sh docs/tests/m0021-exhaustive-match-coverage.sh`, adversarial check, and
`git diff --check`.

Residual risk: diagnostic rendering of missing variant names remains out of
scope for this task.
