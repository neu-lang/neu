# Review: M0018-016

## Review Metadata

- Task ID: `M0018-016`
- Milestone: `M0018`
- main-task review: `main-task review`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0018-016-parser-assignment-statement-metadata.md`
- Milestone file: `docs/milestones/M0018-type-checking-core.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0027-type-checking-core.md`
  - `docs/adr/ADR-0024-expression-statement-pattern-syntax.md`
- Diff or changed files:
  - `crates/newlang/src/parser.rs`
  - `crates/newlang/tests/parser.rs`
  - `docs/tasks/M0018-016-parser-assignment-statement-metadata.md`
- Test results:
  - `cargo test --workspace --all-targets`: pass, 153 tests
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `sh docs/tests/m0018-type-checking-core-accepted.sh`: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0018-016-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests unless main-task review approval is recorded.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passed as the final gate.

## Findings

No blocking findings.

## Spec Compliance

- Relevant spec sections:
  - `ADR-0024: Expression, Statement, And Pattern Syntax`
  - `ADR-0027: Type Checking Core`
- Relevant ADRs:
  - `ADR-0024`
  - `ADR-0027`
- Compliance decision: `compliant`
- Notes:
  - ADR-0027 includes assignment statements where both sides have known types. This task only exposes parser metadata needed by a later type-checking task.
  - The implementation does not check assignment compatibility, resolve names, infer types, or add target legality rules.

## Test Review

- Test-first evidence:
  - Pre-implementation `cargo test --workspace --all-targets` failed because `ParseOutput` had no `assignment_statements` field.
- Ordinary test result:
  - Pass, `cargo test --workspace --all-targets`, 153 tests.
- Adversarial test result:
  - Pass, harness ran after ordinary tests and soundness report found no scope escape.
- CI result:
  - Pass: formatting, clippy, workspace tests, M0018 acceptance script, and M0002 workspace CI script.

## Scope Review

- In scope:
  - Added `ParsedAssignmentStatement` metadata with statement, target expression, and value expression nodes.
  - Added parser tests for valid assignments, source order, and malformed/non-assignment exclusion.
- Out of scope:
  - None observed.

## Architecture And Maintainability Review

- Maintainability concerns:
  - None. The metadata follows existing parser side-table patterns.
- Simplicity concerns:
  - None. The implementation records metadata only at the existing successful assignment parse point.
- Required specialty reviews:
  - `main-task adversarial check`

## Decision

Approve. The task provides assignment statement parser input metadata without introducing type-checking or safety semantics.

## Follow-Up

- Add a later M0018 task to check exact assignment compatibility using this metadata and existing expression type side tables.
