# Review: M0021-003

**APPROVE**

The diff is confined to ADR-0033 parser metadata: `when` subjects and ordered
qualified/wildcard arms receive AST nodes and side-table records. It neither
resolves variants nor implements duplicate detection, coverage, subject
validation, or arm-result typing. Test-first failure is recorded in the task;
the implementation does not weaken tests.

Checked against `docs/SPEC.md`, "ADR-0033: Bootstrap Sealed Sums And
Exhaustive Match", and M0021's parser-metadata scope. Passed: focused parser
tests, metadata validator, formatter, strict Clippy, full workspace tests, and
the post-test adversarial assessment. Residual risk is the explicitly deferred
semantic phase, not this parser slice.
