# Review: M0019-011

## Review Metadata

- Task ID: `M0019-011`
- Milestone: `M0019`
- Reviewer: `Reviewer`
- Review Date: `2026-07-10`
- Decision: `approve`

## Inputs Read

- Task file: `docs/tasks/M0019-011-local-binding-resolution-identity.md`
- Milestone file: `docs/milestones/M0019-nullability-and-flow-typing.md`
- Specification: `docs/SPEC.md`
- ADRs:
  - `docs/adr/ADR-0028-nullability-and-flow-typing.md`
- Diff or changed files:
  - `crates/newlang/src/name_resolution.rs`
  - `crates/newlang/tests/name_resolution.rs`
  - `docs/tests/m0019-local-binding-resolution-identity.sh`
  - `docs/tasks/M0019-011-local-binding-resolution-identity.md`
  - `docs/tasks/soundness/M0019-011-soundness.md`
- Test results:
  - `cargo test -p newlang --test name_resolution m0019_local_binding_resolution_identity`: pass
  - `cargo test -p newlang --test name_resolution`: pass
  - `sh docs/tests/m0019-local-binding-resolution-identity.sh`: pass
  - `cargo fmt --all --check`: pass
  - `cargo clippy --workspace --all-targets -- -D warnings`: pass
  - `cargo test --workspace --all-targets`: pass, 191 tests
  - M0019 docs validators: pass
  - `sh docs/tests/m0002-workspace-ci.sh`: pass
- Adversarial report:
  - `docs/tasks/soundness/M0019-011-soundness.md`

## Required Checks

- [x] Task references exactly one milestone.
- [x] Scope matches the milestone.
- [x] Out-of-scope items were not changed.
- [x] Tests were created before implementation.
- [x] Pre-implementation test failure was recorded.
- [x] Implementation did not weaken or delete tests.
- [x] Ordinary tests passed before adversarial tests.
- [x] Adversarial check ran after ordinary tests.
- [x] Output was compared against `docs/SPEC.md`.
- [x] Output was compared against the milestone acceptance criteria.
- [x] CI passes as the final gate.

## Findings

No blocking, high, medium, or low findings.

## Spec Compliance

- Relevant spec sections:
  - `SPEC.md` lexical scope and local shadowing rules.
- Relevant ADRs:
  - `ADR-0028` local binding identity and shadowing rules.
- Compliance decision: `compliant`
- Notes:
  - Successful local lookup now preserves the exact `LocalBinding` used for each name-expression reference.
  - The existing symbol table, lookup visibility, source ordering, and unresolved-name diagnostics are unchanged.
  - No smart cast, branch walk, nullable-use diagnostic, or mutation invalidation behavior was added.

## Test Review

- Test-first evidence:
  - Before implementation, focused compilation failed because `resolved_local_bindings` did not exist and the docs validator failed because `ResolvedLocalBinding` was absent.
- Fixture correction:
  - Reviewer approves adding the required terminator after a non-trailing `if` expression. The correction changed only fixture validity and did not weaken identity assertions.
- Ordinary test result:
  - pass
- Adversarial test result:
  - pass
- CI result:
  - pass

## Scope Review

- In scope:
  - Added passive reference-to-local-binding records to existing local reference binding output.
  - Added exact identity, nested shadowing, source-order, and unresolved-use tests.
  - Added a focused docs validator.
- Out of scope:
  - None observed. No spec, ADR, parser, lookup semantics, type checking, diagnostics semantics, or examples were changed.

## Architecture And Maintainability Review

- Maintainability concerns:
  - none
- Simplicity concerns:
  - none; a small record vector reuses the existing binding result and avoids changing the general resolution model.
- Required specialty reviews:
  - `Adversarial Engineer`
  - `Spec Compliance Auditor`
  - `Simplicity Guardian`

## Decision

Approve.

## Follow-Up

- The next M0019 task may use these records to assign per-use refined expression types only when the use resolves to the refined binding.
