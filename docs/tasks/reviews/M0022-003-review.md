# Review: M0022-003

APPROVE

Inputs read: `docs/tasks/M0022-003-ownership-transfer-sites.md`,
`docs/SPEC.md`, `docs/adr/ADR-0035-bootstrap-ownership-and-move-analysis.md`,
`docs/milestones/M0022-ownership-and-move-analysis.md`, and the changed
ownership files.

The implementation is scoped to transfer-origin collection for ADR-0035 local
initializer and assignment move sites. It ignores copyable, unsupported,
unresolved, and non-local-name sources. It does not diagnose use-after-move or
perform ordering, branch, call, return, capture, borrow, or destructor analysis.

Validation passed: `cargo fmt --all --check`, `cargo clippy --workspace
--all-targets -- -D warnings`, `cargo test --workspace --all-targets`,
`sh docs/tests/m0022-ownership-transfer-sites.sh`, adversarial check, and
`git diff --check`.

Residual risk: invalid later-use detection remains a future M0022 task.
