# ADR-0056 Simplicity Review

Decision: approve. Reusing the existing module, package, and declaration
identity facts avoids a parallel backend registry and postpones only the
encoding detail that belongs at the object boundary.
